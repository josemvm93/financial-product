import { BaseControlDirective } from './base-control.directive';

describe('BaseControlDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseControlDirective();
    expect(directive).toBeTruthy();
  });
});
